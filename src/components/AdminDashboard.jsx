import { useMemo, useState } from 'react'
import { BarChart3, Building2, ClipboardList, GitBranch, Plus, Settings2, ShieldCheck, Trash2, Workflow } from 'lucide-react'
import { Bar, BarChart, CartesianGrid, Cell, ComposedChart, Line, LineChart, Pie, PieChart, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Badge, Button, Card, Input, Label } from './ui'

const overview = [{ label: 'Total requests', value: '248', change: '+12.4%', icon: ClipboardList, color: 'bg-[#e7f3ee] text-teal' }, { label: 'Approved', value: '164', change: '66.1%', icon: ShieldCheck, color: 'bg-[#e4f5ec] text-[#267151]' }, { label: 'Rejected', value: '31', change: '12.5%', icon: GitBranch, color: 'bg-[#fff0ed] text-coral' }, { label: 'Pending', value: '53', change: '21.4%', icon: Workflow, color: 'bg-[#fff1d7] text-[#9b6914]' }]
const byType = [{ name: 'Leave', requests: 76 }, { name: 'Event', requests: 58 }, { name: 'Late arrival', requests: 42 }, { name: 'Hostel', requests: 38 }, { name: 'Travel', requests: 34 }]
const overTime = [{ name: 'Aug 01', requests: 18 }, { name: 'Aug 05', requests: 27 }, { name: 'Aug 10', requests: 22 }, { name: 'Aug 15', requests: 35 }, { name: 'Aug 20', requests: 31 }, { name: 'Aug 25', requests: 44 }, { name: 'Aug 30', requests: 39 }]
const statusData = [{ name: 'Approved', value: 164, color: '#2d8567' }, { name: 'Pending', value: 53, color: '#e4aa3f' }, { name: 'Rejected', value: 31, color: '#ef846d' }]
const departments = [{ name: 'Computer science', pending: 22 }, { name: 'Mechanical', pending: 16 }, { name: 'Business', pending: 11 }, { name: 'Design', pending: 8 }]
const initialConfig = { Colleges: ['Northbridge College', 'Lakeside Institute'], Departments: ['Computer Science', 'Business Administration', 'Mechanical Engineering'], Years: ['First year', 'Second year', 'Third year', 'Fourth year'], Sections: ['CSE-A', 'CSE-B', 'MBA-A'], 'Authority assignments': ['Dean → Leave requests', 'HOD → Event participation'], Workflows: ['Student → HOD → Admin', 'Student → Warden → Security'], 'Permission types': ['Leave campus', 'Event participation', 'Late arrival'] }

function ConfigTable({ title, items, onAdd, onDelete }) { return <div className="rounded-xl border border-[#e2eee9] bg-white"><div className="flex items-center justify-between border-b border-[#edf3f0] px-4 py-3"><h3 className="text-sm font-bold text-ink">{title}</h3><Button variant="ghost" size="icon" aria-label={`Add ${title}`} onClick={() => onAdd(title)}><Plus size={16} /></Button></div><div className="divide-y divide-[#edf3f0]">{items.map((item) => <div key={item} className="flex items-center justify-between gap-3 px-4 py-3 text-sm text-[#58706c]"><span>{item}</span><Button variant="ghost" size="icon" aria-label={`Delete ${item}`} onClick={() => onDelete(title, item)} className="h-8 w-8 text-[#9aacaa] hover:text-coral"><Trash2 size={14} /></Button></div>)}</div></div> }

export default function AdminDashboard() {
  const [config, setConfig] = useState(initialConfig)
  const [activeTab, setActiveTab] = useState('Overview')
  const tabs = ['Overview', 'Academic structure', 'Authorities & workflows']
  const averagePending = useMemo(() => departments.reduce((total, item) => total + item.pending, 0) / departments.length, [])
  function addItem(title) { const value = window.prompt(`Add ${title.slice(0, -1)}`); if (value?.trim()) setConfig((current) => ({ ...current, [title]: [...current[title], value.trim()] })) }
  function deleteItem(title, item) { setConfig((current) => ({ ...current, [title]: current[title].filter((value) => value !== item) })) }

  return <div className="space-y-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-sm font-bold uppercase tracking-[0.18em] text-coral"><BarChart3 size={16} />Admin command center</p><h1 className="font-display text-3xl font-extrabold tracking-tight text-ink sm:text-4xl">Permission overview</h1><p className="mt-2 text-[#718783]">A live view of campus requests, bottlenecks, and operating rules.</p></div><div className="flex rounded-xl border border-[#dce9e4] bg-white p-1">{tabs.map((tab) => <button key={tab} type="button" onClick={() => setActiveTab(tab)} className={`rounded-lg px-3 py-2 text-xs font-bold transition ${activeTab === tab ? 'bg-teal text-white' : 'text-[#718783] hover:bg-mint/50'}`}>{tab}</button>)}</div></div>{activeTab === 'Overview' ? <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{overview.map(({ label, value, change, icon: Icon, color }) => <Card key={label} className="p-5"><div className="flex items-start justify-between"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${color}`}><Icon size={19} /></div><span className="text-xs font-bold text-[#5d987a]">{change}</span></div><p className="mt-5 text-sm text-[#718783]">{label}</p><p className="mt-1 font-display text-3xl font-extrabold text-ink">{value}</p></Card>)}</div><div className="grid gap-5 xl:grid-cols-2"><Card className="p-5"><h2 className="font-display text-lg font-extrabold text-ink">Requests by permission type</h2><p className="mb-4 text-sm text-[#8aa09b]">Volume this month</p><ResponsiveContainer width="100%" height={260}><BarChart data={byType} margin={{ top: 5, right: 10, left: -20, bottom: 0 }}><CartesianGrid stroke="#e8f0ed" vertical={false} /><XAxis dataKey="name" tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip cursor={{ fill: '#f4faf7' }} /><Bar dataKey="requests" fill="#0d4f4a" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></Card><Card className="p-5"><h2 className="font-display text-lg font-extrabold text-ink">Requests over time</h2><p className="mb-4 text-sm text-[#8aa09b]">Submissions in August</p><ResponsiveContainer width="100%" height={260}><LineChart data={overTime} margin={{ top: 5, right: 10, left: -20, bottom: 0 }}><CartesianGrid stroke="#e8f0ed" vertical={false} /><XAxis dataKey="name" tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip /><Line type="monotone" dataKey="requests" stroke="#ef846d" strokeWidth={3} dot={{ fill: '#ef846d', r: 4 }} /></LineChart></ResponsiveContainer></Card><Card className="p-5"><h2 className="font-display text-lg font-extrabold text-ink">Status distribution</h2><p className="mb-4 text-sm text-[#8aa09b]">Current request outcomes</p><div className="flex items-center gap-4"><ResponsiveContainer width="55%" height={220}><PieChart><Pie data={statusData} dataKey="value" nameKey="name" innerRadius={58} outerRadius={86} paddingAngle={4}>{statusData.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer><div className="space-y-3">{statusData.map((item) => <div key={item.name} className="flex items-center gap-2 text-sm"><span className="h-2.5 w-2.5 rounded-full" style={{ backgroundColor: item.color }} /><span className="text-[#718783]">{item.name}</span><strong className="text-ink">{item.value}</strong></div>)}</div></div></Card><Card className="p-5"><h2 className="font-display text-lg font-extrabold text-ink">Pending by department</h2><p className="mb-4 text-sm text-[#8aa09b]">Average: {averagePending.toFixed(1)} pending requests</p><ResponsiveContainer width="100%" height={220}><ComposedChart data={departments} margin={{ top: 5, right: 10, left: -20, bottom: 0 }}><CartesianGrid stroke="#e8f0ed" vertical={false} /><XAxis dataKey="name" tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: '#829793', fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip /><Bar dataKey="pending" fill="#e4aa3f" radius={[6, 6, 0, 0]} /><ReferenceLine y={averagePending} stroke="#ef846d" strokeDasharray="5 5" /></ComposedChart></ResponsiveContainer></Card></div></> : <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{Object.entries(config).filter(([title]) => activeTab === 'Academic structure' ? ['Colleges', 'Departments', 'Years', 'Sections'].includes(title) : ['Authority assignments', 'Workflows', 'Permission types'].includes(title)).map(([title, items]) => <ConfigTable key={title} title={title} items={items} onAdd={addItem} onDelete={deleteItem} />)}</div>}</div>
}
